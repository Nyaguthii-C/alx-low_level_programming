#include "main.h"

/**
 * mul -multiplies to integers
 * @a: value of integer to be multiplied
 * @b: value of integer to be multiplied
 *
 * Return: value of multiplication
 */

int mul(int a, int b)
{
	mul(a, b);
	{
		return (a * b);
	}
}
