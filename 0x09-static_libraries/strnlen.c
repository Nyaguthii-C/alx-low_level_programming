#include "main.h"

/**
* _strlen - funds length of string
* @s: string
*
* Return: the counter;
*/
int _strlen(char *s)
{
	int i = 0;
	int n;

	for (i < n; *s != '\0'; ++s)
	{
		i++;
		return (i);
	}
}
