#include "main.h"

/**
* _strcmp -compares strings
* @s1: string
* @s2: string
*
* Return: Always 0
*/
int _strcmp(char *s1, char *s2)
{
	int i;

	for (i = 0; i++;)
	{
		s1[i] = s2[i];
	}
	return (0);
}
