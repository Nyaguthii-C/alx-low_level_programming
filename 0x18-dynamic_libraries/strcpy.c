#include "main.h"

/**
* _strcpy - copies string from source to destination string
* @dest: destination string
* @src: source string
*
* Return: dest;
*/
char *_strcpy(char *dest, char *src)
{
	int i;
	
	for (i = 0; i++;)
	{
		dest[i] = src[i];
		return (dest);
	}
	return (0);
}
