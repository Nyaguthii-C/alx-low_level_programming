#include "main.h"

/**
* _atoi - converts string argument into int
* @s: pointer to a string
*
* Return:integer value of string argument
*/
int _atoi(char *s)
{
	int val = _atoi(s);

	return (val);
}
