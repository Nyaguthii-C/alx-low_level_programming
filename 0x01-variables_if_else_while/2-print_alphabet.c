#include <stdio.h>

/**
 * Main - prints lowercase alphabet
 *
 * Return: 0
 */
int main(void)
{
	char ch = 'a';
	putchar("alphabet in lowercase\n");
	while (char <= 'z')
	{
		putchar("%c" , ch);
		ch++;
	}	
	return (0);
}	
